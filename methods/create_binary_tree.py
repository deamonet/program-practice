from tree_node import TreeNode



def create_binary_tree(elements):
    root = TreeNode(elements[0])
    left = TreeNode(elements[1])
    right = TreeNode(elements[2])    
    

def create(node, deep, order):
    node.val = elements[2**deep-1+order]
    left = TreeNode
